services:
  netbox-prometheus-sd:
    build: .
    ports:
      - "9099:9099"
    environment:
      VERIFY_SSL: "true"
      LOG_LEVEL: "DEBUG"
    env_file:
      - local.env
    restart: unless-stopped
